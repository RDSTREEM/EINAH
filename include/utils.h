#ifndef UTILS_H
#define UTILS_H

#include <string>

/**
 * @brief Check if a character is skippable whitespace (space, newline, or tab).
 *
 * @param c The character to check.
 * @return true if the character is skippable, false otherwise.
 */
bool isSkippable(char c);

#endif